# Build Shared Features

This folder contains shared build tools and configurations used across all packages in the monorepo.

## Packages

- **ts-config**: Shared TypeScript configurations
- **ts-lint**: ESLint and Prettier configurations
- **vitest-config**: Vitest testing configuration
- **test-coverage**: Test coverage aggregation
- **template-build**: CLI tool for generating features from templates

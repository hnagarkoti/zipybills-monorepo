/**
 * @zipybills/ui-query – TanStack React Query configuration
 *
 * Provides QueryClient, QueryClientProvider wrapper,
 * and shared query key factories.
 */

export { QueryProvider, queryClient } from './provider';
export { queryKeys } from './keys';

/**
 * Default Theme Bundles
 *
 * System-provided themes for all 6 layers.
 * These are the built-in themes that ship with FactoryOS.
 * They serve as the baseline and can be overridden by tenant/role/user configs.
 */

export { BASE_THEMES } from './base-themes';
export { ROLE_THEMES } from './role-themes';
export { ENVIRONMENT_THEMES } from './environment-themes';
export { COMPLIANCE_THEMES } from './compliance-themes';
export { DEFAULT_TOKENS } from './default-tokens';

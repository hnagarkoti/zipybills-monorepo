/**
 * Language settings route – /settings/language
 */
import React from 'react';
import { LanguageSettings } from '@zipybills/factory-settings-frontend';

export default function LanguageRoute() {
  return <LanguageSettings />;
}

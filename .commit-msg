feat: API versioning, feature flags, Expo Router, UI component library

Phase 6: Production-grade feature flag engine + API versioning

Backend:
- Feature registry: ENABLED/DISABLED/DEPRECATED states, Express middleware, Admin API, React hooks
- API Gateway: Versioned routes (/api/v1/*) with backward-compat, scoped feature gates
- SDK client: apiVersion injection, buildQuery tuple types

Frontend:
- Expo Router file-based routing with auth guard + feature flag filtering
- Route pages: dashboard, machines, plans, shifts, downtime, reports, users
- UI Components: Alert, Avatar, Badge, Button, Card, Divider, EmptyState, ErrorBoundary, IconButton, Input, Loading, NotFoundPage, PageHeader, ProgressBar, ServerErrorPage, StatCard, StatusDot
- Zustand stores (auth, app state), TanStack React Query provider
- AppShell layout with Sidebar + Header + BottomNav

Dependency fixes:
- react-native-reanimated ~4.1.1, screens ~4.16.0, gesture-handler ~2.28.0 (Expo SDK 54)
- Metro config: pnpm workspace resolution (21 packages + subpath exports)

Validation: Backend 46/46, TypeScript 0 errors, iOS 3257 modules, Web HTTP 200

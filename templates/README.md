# Templates

Feature templates for quick scaffolding of new features.

## Template Feature

The template-feature directory contains a base template for creating new features. Use the `pnpm generate-feature` command to generate a new feature from this template.
